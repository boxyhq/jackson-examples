<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <title>BoxyHQ Enterprise SSO</title>
    <link rel="stylesheet" href="./styles/style.css" />
  </head>
  <body>
    <%- include ("partials/header.ejs") %>
    <div class="mx-auto mt-10 max-w-7xl">
      <div class="max-w-[50%]">
        <div class="mb-5">
          <% if(hasConnection){ %>
          <span class="mr-2 rounded bg-green-100 px-2.5 py-0.5 text-sm font-medium text-green-800"
            >SAML SSO Enabled</span
          >
          <% } else{ %>
          <span class="mr-2 rounded bg-red-100 px-2.5 py-0.5 text-sm font-medium text-red-800"
            >SAML SSO Not Enabled</span
          >
          <% } %>
        </div>
        <div class="rounded border border-gray-200 bg-white py-5 px-5">
          <form class="space-y-3" method="POST" action="/settings">
            <label htmlFor="rawMetadata" class="block text-sm">XML Metadata</label>
            <textarea
              name="rawMetadata"
              id="rawMetadata"
              placeholder="Paste the XML Metadata"
              class="block w-full appearance-none rounded border border-gray-300 text-sm placeholder-gray-400 focus:outline-none focus:ring-indigo-500"
              required
              rows="10"></textarea>
            <button
              type="submit"
              class="w-full rounded border border-transparent bg-indigo-600 px-4 py-2 text-sm font-medium text-white focus:outline-none">
              Save Changes
            </button>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
